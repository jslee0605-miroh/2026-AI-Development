{
  "tools": [
    {
      "name": "list_docs",
      "description": "List available documents in the local docs folder.",
      "input_schema": { "type": "object", "properties": {}, "required": [] }
    },
    {
      "name": "read_doc",
      "description": "Read a document by filename from the docs folder.",
      "input_schema": {
        "type": "object",
        "properties": { "filename": { "type": "string" } },
        "required": ["filename"]
      }
    },
    {
      "name": "search_docs",
      "description": "Search documents for a keyword and return short snippets.",
      "input_schema": {
        "type": "object",
        "properties": { "query": { "type": "string" }, "top_k": { "type": "integer" } },
        "required": ["query"]
      }
    },
    {
      "name": "summarize_market_signals",
      "description": "Compute simple aggregates from market_signals.csv (by channel and campaign).",
      "input_schema": { "type": "object", "properties": {}, "required": [] }
    },
    {
      "name": "write_brief",
      "description": "Write the final research brief markdown to data/outputs/research_brief.md.",
      "input_schema": {
        "type": "object",
        "properties": { "markdown": { "type": "string" } },
        "required": ["markdown"]
      }
    }
  ]
}

